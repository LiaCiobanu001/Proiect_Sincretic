#include <iostream>
int main()
{
    int a, b;
    std :: cin >> a >> b;
    while(a != b)
        if(a > b)
            a -= b;
        else
            a -= b;
    std :: cout << a << std :: endl;
    return 0;
}
